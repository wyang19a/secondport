import React from 'react'

const BoxedLayout = ({children}) => {
    return (
        <div className="boxed-page">
            {children}
        </div>
    )
}

export default BoxedLayout
