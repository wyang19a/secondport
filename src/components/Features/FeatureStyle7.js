import React from 'react'

const FeatureStyle7 = ({title, subtitle, icon}) => {
    return (
        <div className="feature-7">
            <a href="#">{icon}</a>
            <h4>{title}</h4>
            <span>{subtitle}</span>
        </div>
    )
}

export default FeatureStyle7
